import { PriceInRonPipe } from './price-in-ron.pipe';

describe('PriceInRonPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceInRonPipe();
    expect(pipe).toBeTruthy();
  });
});
